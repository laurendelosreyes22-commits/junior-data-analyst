name: 'junior_data_analyst'
version: '1.0.0'
config-version: 2

profile: 'junior_data_analyst'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  junior_data_analyst:
    staging:
      +materialized: view
    marts:
      +materialized: table
